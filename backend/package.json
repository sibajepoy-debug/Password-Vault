{
  "name": "password-vault-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "dev": "json-server --watch db.json --port 3001 & nodemon server.js",
    "json-server": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "http-proxy-middleware": "^4.0.0",
    "json-server": "^0.17.4",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
